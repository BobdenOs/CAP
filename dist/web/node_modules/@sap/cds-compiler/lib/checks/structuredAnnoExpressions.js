import __IMPORT_1__ from '/web/node_modules/@sap/cds-compiler/lib/model/csnUtils.js'
import __IMPORT_0__ from '/web/node_modules/@sap/cds-compiler/lib/base/builtins.js'

const require = function() {throw Object.assign(new Error('require in browser'),{code:'MODULE_NOT_FOUND'})}
require.resolve = function(id) { 
  if(id[0] === '.') return new URL(id, import.meta.url).pathname
  throw Object.assign(new Error('require.resolve in browser'),{ code: 'MODULE_NOT_FOUND' }) 
}
export default (function(exports, module, require, __dirname,__filename) {
if(module.__loaded__) { return module.exports }
module.__loaded__ = true
module.exports = exports;
'use strict';

const { isBuiltinType } = __IMPORT_0__();
const { transformAnnotationExpression } = __IMPORT_1__();
/**
 *
 * @param {object} member
 */
function checkAnnotationExpression( member, _memberName, _prop, path ) {
  Object.keys(member).filter(pn => pn[0] === '@').forEach((anno) => {
    transformAnnotationExpression(member, anno, {
      ref: (elemref, __prop, _ref, refPath) => {
        const { art, scope } = this.csnUtils.inspectRef(refPath);
        if (scope !== '$magic' && art) {
          const ft = this.csnUtils.getFinalTypeInfo(art.type);
          if (!isBuiltinType(ft?.type) && !ft?.items)
            this.error('odata-anno-xpr-ref', refPath, { anno, elemref, '#': 'flatten_builtin_type' });
        }
      },
    }, path);
  });
}

module.exports = {
  checkAnnotationExpression,
};

return module.exports
}).bind(globalThis,{},{},require,new URL('.', import.meta.url).pathname,new URL('', import.meta.url).pathname)